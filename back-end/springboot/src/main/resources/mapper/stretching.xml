<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssafy.stretching">

	<select id="getVideoPath"
		resultType="com.ssafy.edu.vue.dto.Video">
		select video_id, title, file, description
		from video
		where video_id=#{video_id}
	</select>

	<select id="getPartList"
		resultType="com.ssafy.edu.vue.dto.Part">
		select part_id, name
		from part
	</select>
	
	<select id="getPartVideoList"
		parameterType="java.lang.Integer"
		resultType="com.ssafy.edu.vue.dto.Video">
		select v.video_id, title, file, thumbnail 
		from video v, stretching s 
		where part_id=#{part_id} and v.video_id = s.video_id
	</select>
	
	<select id="getAllVideoList"
		resultType="com.ssafy.edu.vue.dto.Video">
		select video_id, title, file, thumbnail, description
		from video v
	</select>
	
	<select id="getVideoPart"
		parameterType="java.lang.Integer"
		resultType="java.lang.String">
		select p.name
		from part p, stretching s
		where video_id=#{video_id} and p.part_id = s.part_id
	</select>
	
	<select id="getBeforeAccuracy"
		parameterType="com.ssafy.edu.vue.dto.Result"
		resultType="java.lang.Float">
		select accuracy
		from result
		where video_id=#{video_id} and member_id=#{member_id}
		order by datetime desc
		limit 1
	</select>
	
	<insert id="setResult"
		parameterType="java.lang.Integer">
		insert into result(member_id, video_id, accuracy, point, datetime) 
		values (#{member_id}, #{video_id}, #{accuracy}, #{point}, now())
	</insert>
	
	<select id="getStretchingCnt"
		resultType="java.lang.Integer">
		select count(*) from result 
		where left(datetime,10)=left(now(),10)
	</select>
	
	<select id="getStretchingMem"
		resultType="java.lang.Integer">
		select count(*) 
		from (select distinct member_id from result 
		where left(datetime,10)=left(now(),10)) today_member
	</select>
	
	<select id="getStretchingTime"
		parameterType="java.lang.Integer"
		resultType="java.lang.Integer">
		select time from video where video_id=#{video_id}
	</select>
	
	<select id="getVideoList"
		resultType="java.lang.Integer">
		select video_id from video order by video_id
	</select>
	
	<select id="getStretchingAllCnt"
		parameterType="java.lang.Integer"
		resultType="java.lang.Integer">
		select count(*) from result where video_id=#{video_id}
	</select>
</mapper>
